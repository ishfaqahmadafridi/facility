import 'package:flutter/material.dart';

class MultiLoadingView extends StatelessWidget {
  const MultiLoadingView({super.key});

  @override
  Widget build(BuildContext context) => const Center(child: CircularProgressIndicator());
}
