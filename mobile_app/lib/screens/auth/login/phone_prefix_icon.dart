import 'package:flutter/material.dart';

class PhonePrefixIcon extends StatelessWidget {
  const PhonePrefixIcon({super.key});

  @override
  Widget build(BuildContext context) => const Icon(Icons.phone);
}
