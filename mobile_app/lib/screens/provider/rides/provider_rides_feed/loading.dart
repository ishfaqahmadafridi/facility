import 'package:flutter/material.dart';
import '../components/loading_view.dart' as base;

class LoadingView extends StatelessWidget {
  const LoadingView({super.key});

  @override
  Widget build(BuildContext context) => const base.LoadingView();
}
