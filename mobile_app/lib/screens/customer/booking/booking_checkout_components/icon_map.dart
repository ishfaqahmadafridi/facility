import 'package:flutter/material.dart';

class IconMap extends StatelessWidget {
  const IconMap({super.key});

  @override
  Widget build(BuildContext context) => const Icon(Icons.map, size: 40, color: Colors.blue);
}
