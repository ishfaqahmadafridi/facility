import 'package:flutter/material.dart';

class Placeholder3 extends StatelessWidget {
  const Placeholder3({super.key});

  @override
  Widget build(BuildContext context) => Container(height: 60, color: Colors.grey.shade50);
}
