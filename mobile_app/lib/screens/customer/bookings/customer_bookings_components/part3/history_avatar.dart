import 'package:flutter/material.dart';

class HistoryAvatar3 extends StatelessWidget {
  const HistoryAvatar3({super.key});

  @override
  Widget build(BuildContext context) => const CircleAvatar(radius: 18, child: Icon(Icons.history));
}
