import 'package:flutter/material.dart';

class RideAvatar2 extends StatelessWidget {
  const RideAvatar2({super.key});

  @override
  Widget build(BuildContext context) => const CircleAvatar(radius: 20, child: Icon(Icons.person));
}
