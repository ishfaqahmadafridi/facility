import 'package:flutter/material.dart';

class DividerSmall extends StatelessWidget {
  const DividerSmall({super.key});

  @override
  Widget build(BuildContext context) => const Divider(height: 32);
}
