import 'package:flutter/material.dart';

class ProviderTrailing extends StatelessWidget {
  const ProviderTrailing({super.key});

  @override
  Widget build(BuildContext context) => const Icon(Icons.chevron_right);
}
