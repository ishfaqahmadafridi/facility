name: facility
description: A new Flutter project for the Service Bidding Marketplace.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  provider: ^6.1.5+1
  jwt_decoder: ^2.0.1
  flutter_secure_storage: ^9.0.0
  image_picker: ^1.2.2
  web_socket_channel: ^3.0.3
  record: ^6.2.0
  audioplayers: ^6.6.0
  permission_handler: ^12.0.1
  path_provider: ^2.1.5
  agora_rtc_engine: ^6.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
